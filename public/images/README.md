# public/images/

General site images — logos, hero images, team photos, etc.

Optimise all images before committing:
- Use WebP format for photos
- Use SVG for logos and icons
- Aim for < 200KB per image (use Next.js `<Image>` component for automatic optimisation)
