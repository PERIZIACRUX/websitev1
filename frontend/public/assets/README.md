# public/assets/

Static assets — fonts, icons, and other resources loaded directly.

Note: Prefer loading fonts via `next/font` (Google Fonts or local) rather than placing
font files here, to take advantage of Next.js font optimisation.
