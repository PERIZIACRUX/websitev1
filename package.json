{
  "name": "crux-perizia-platform",
  "version": "0.1.0",
  "description": "CRUX × PERIZIA — Unified college fest platform. CRUX is the cultural fest; PERIZIA is the academic fest with participant registration.",
  "private": true,
  "scripts": {
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev:backend": "npm run dev --workspace=backend",
    "build:shared": "npm run build --workspace=shared",
    "build:frontend": "npm run build --workspace=frontend",
    "build:backend": "npm run build:shared && npm run build --workspace=backend",
    "test": "npm run test:integration --workspace=backend",
    "type-check": "npm run type-check --workspaces --if-present"
  },
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ]
}
